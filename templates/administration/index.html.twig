{% extends 'base.html.twig' %}

{% block title %}Administration{% endblock %}

{% block body %}

    <div class="containerGrid">
        <h2>Administration</h2>

        <div class="action-btn">
            <button class="Admin-btn" type="submit"><a class="btn-link" href="{{path('article')}}">Ajouter un article</a></button>
            <button class="Admin-btn" type="submit"><a class="btn-link" href="{{path('contact')}}">Contact</a></button>  
        </div>

        <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Titre</th>
                <th>Catégorie</th>
                <th>Auteur</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for article in articles %}
            <tr>
                <td class="rowTable">{{article.id}}</td>
                <td class="rowTable">{{article.title}}</td>
                <td class="rowTable">{{article.getIdCategorie.name}}</td>
                <td class="rowTable">{{article.getIdUser.username}}</td>
                <form action="" method="POST">
                    <td><button class="Delete-btn" type="submit" name="delete_article" value={{article.id}}>Supprimer</button> </td>
                </form>
            </tr>
        {# {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr> #}
        {% endfor %}
        </tbody>
    </table>
        
    </div>
    

    
{% endblock %}