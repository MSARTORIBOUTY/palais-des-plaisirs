{% extends 'base.html.twig' %}

{% block title %}Utilisateurs{% endblock %}

{% block body %}

    <div class="container">
        <h2>Utilisateurs</h2>

        <table class="table">
            <thead>
                <tr>
                    <th>Id</th>
                    <th>Pseudo</th>
                    <th>Email</th>
                    <th>Rôle</th>
                    <th colspan="2">Actions</th>
                </tr>
            </thead>
            <tbody>
                {# {% for  %} #}
                    <tr>
                        <td class="rowTable">1</td>
                        <td class="rowTable">TheSleepyOtter</td>
                        <td class="rowTable">xsedrcftvgybhun@gmail.com</td>
                        <td class="rowTable">
                            <select name="role" class="role-select GridLine">
                                <option value="admin">admin</option>
                                <option value="user">utilisateur</option>
                                <option value="autor">auteur</option>
                            </select>
                        </td>
                        <td><button class="Modif-btn" type="submit">Modifier</button></td>
                        <td><button class="Delete-btn" type="submit">Supprimer</button> </td>
                    </tr>
                {# {% else %}
                    <tr>
                        <td colspan="4">no records found</td>
                    </tr>
                {% endfor %} #}
            </tbody>
        </table>

        <div class="wrapper">
            <button class="contact-btn" type="submit">Enregistrer</button> 
        </div>
    </div>
    

    
{% endblock %}